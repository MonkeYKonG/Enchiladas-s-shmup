#include "Button.hpp"

namespace my
{
	Button::Button()
	{}

	Button::~Button()
	{}

	/*bool my::Button::IsIntersect(const sf::Vector2f & point) const noexcept
	{
		if (m_sprite && m_sprite->IsIntersect(point))
			return (true);
		if (m_text && m_text->IsIntersect(point))
			return (true);
		return false;
	}

	bool my::Button::IsIntersect(const sf::IntRect & square) const noexcept
	{
		if (m_sprite && m_sprite->IsIntersect(square))
			return (true);
		if (m_text && m_text->IsIntersect(square))
			return (true);
		return false;
	}*/
}